STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-v2
POSTGRES_URL=

# Postmark transactional email (used by notification-postmark provider)
POSTMARK_API_TOKEN=
POSTMARK_FROM=
# Storefront URL embedded in password-reset email links
STOREFRONT_URL=

# Optional: push placed orders into the QuickBooks Desktop bridge
QB_SYNC_ORDER_IMPORT_URL=
QB_SYNC_ORDER_IMPORT_TOKEN=

# Optional: WWEX / Unishippers Speedship small-parcel UPS rating.
# Do not put staging credentials in production unless the checkout is explicitly
# in staging/test mode. Production credentials are issued by WWEX after staging
# development is complete.
WWEX_AUTH_URL=
WWEX_API_BASE_URL=
WWEX_CLIENT_ID=
WWEX_CLIENT_SECRET=
WWEX_AUDIENCE=
WWEX_ORIGIN_COMPANY_NAME=Griller's Pride
WWEX_ORIGIN_ADDRESS_1=
WWEX_ORIGIN_ADDRESS_2=
WWEX_ORIGIN_CITY=
WWEX_ORIGIN_STATE=
WWEX_ORIGIN_POSTAL_CODE=
WWEX_ORIGIN_PHONE=
WWEX_ORIGIN_EMAIL=
WWEX_DEFAULT_PACKAGE_WEIGHT_LB=1
WWEX_MAX_PACKAGE_WEIGHT_LB=40
WWEX_DEFAULT_PACKAGE_LENGTH_IN=
WWEX_DEFAULT_PACKAGE_WIDTH_IN=
WWEX_DEFAULT_PACKAGE_HEIGHT_IN=
WWEX_PACKAGE_DIMENSIONS_JSON=

# Optional: enable actual shipment booking and label retrieval after final
# pre-shipment charge succeeds. Keep disabled until production bill-to details
# and staff label handling are verified.
WWEX_BILL_TO_ACCOUNT_NBR=
WWEX_BILL_TO_POSTAL_CODE=
WWEX_BILL_TO_COUNTRY_CODE=US
WWEX_BILL_TO_TYPE=SENDER
WWEX_BOOK_SHIPMENTS_ON_RELEASE=false
WWEX_FETCH_LABEL_ON_RELEASE=true
WWEX_NOTIFICATION_EMAIL=
